# 期刊Dashboard配置示例
# 所有配置已集成到 application.properties 中
# 此文件仅供参考

## 数据库配置
# 在 application.properties 中配置：
# journal.datasource.url=jdbc:mysql://127.0.0.1:3306/paper_db?characterEncoding=utf8mb4&useSSL=false&serverTimezone=UTC
# journal.datasource.username=root
# journal.datasource.password=your_password

## AI配置
# 在 application.properties 中配置：
# ai.api.base-url=https://api.deepseek.com/v3
# ai.api.key=your_api_key_here
# ai.api.model=deepseek-chat
# ai.api.timeout=60

## 使用环境变量覆盖（可选）
# 环境变量可以覆盖 application.properties 中的值
# 格式：大写字母，点号替换为下划线
# 例如：
# JOURNAL_DATASOURCE_PASSWORD=your_password
# AI_API_KEY=your_api_key
